# 📝 WorkList — Detalhamento Técnico de Atividades

Este documento gerencia as tarefas técnicas de baixo nível necessárias para a execução do ecossistema Beaconray.

| ID | Módulo | Descrição Detalhada da Tarefa | Dependências | Complexidade | Status |
| :--- | :--- | :--- | :--- | :---: | :---: |
| **HN-001** | Harness | Instalar o harness spec-driven do projeto: `AGENTS.md` (YAML enxuto), `.specs/SPECS.md`, `.specs/SKILLS.md`, `.specs/skills/`, `.specs/ADRS.md`, `CHANGELOG.md`. Regras always-on de caveman mode em `.cursor/`, `.windsurf/`, `.clinerules/`, `.github/copilot-instructions.md`. | Nenhuma | Baixa | ✅ Concluído |
| **QG-001** | Qualidade | Escrever `.specs/component-quality-spec.md`: gate obrigatório de HTML5 semântico, WCAG 2.2 AA (mapa completo dos 24 critérios), acesso multi-modal (teclado/voz/leitor de tela), orçamento de performance (ultra leve), disciplina de tokens de animação e SEO/GEO. | HN-001 | Média | ✅ Concluído (subconjunto estático completo enforçado em `validate.ts`: tag semântica em evento, nome acessível, `alt`, `tabindex`, `href`, cor/tempo/px cru, **aria-live** (heurístico), **foco em `show`** via `focusOnShow`; `prefers-reduced-motion` coberto globalmente pelo tema; foco em `for`/remoção de item continua aberto, rastreado em `ast-component-spec.md`) |
| **SEO-001** | Qualidade | Output estático (Astro/`qa-html`) passa a renderizar via `componentToTemplate` (texto real embutido no HTML) em vez de `componentToHtml` (placeholder + JS de hidratação) — resolve o problema real de crawler de busca/LLM não verem conteúdo. Regra nova: todo `<a>` precisa de `href`. | CP-002 | Média | ✅ Concluído (ADR-009; verificado com `Button`/`Counter` — texto/itens de lista aparecem de verdade no HTML gerado, sem `<script>`) |
| **SEO-002** | Qualidade | Dados estruturados (schema.org JSON-LD) — campo `schema` opcional no AST, emitido como `<script type="application/ld+json">` no output estático. | SEO-001 | Média | ✅ Concluído (`validate.ts` exige `@context`/`@type`; testado com fixture `Button`, JSON-LD válido confirmado no output gerado) |
| **QG-002** | Qualidade | Escrever `.specs/component-qa-strategy-spec.md`: estratégia de QA em 3 camadas (Playwright+axe-core, Storybook, Cypress) documentada — implementação entra conforme QA-001/QA-002/QA-003 abaixo. | QG-001 | Baixa | ✅ Concluído (documentado; nenhuma camada implementada ainda) |
| **CP-001** | Compiler| Script Node.js isolado (Parser Engine) que consome AST de componente (`.specs/ast-component-spec.md`) e reconstrói o arquivo bruto no formato Mitosis `.lite.tsx`. Hoje consome AST escrito à mão (Studio ainda não existe); consumirá o mesmo formato vindo do Postgres/Studio depois. | Nenhuma (AST hand-authored) | Crítica | ✅ Concluído (v0 + v1: props→atributos/texto, state, eventos, condicional `Show`, loop `For` — validado ponta a ponta com fixtures `Button` e `Counter`; foco em `show`/`for` é item em aberto, não bloqueia) |
| **CP-002** | Compiler| Pipeline de compilação programática do Mitosis via API do Node.js, geradores de código pra `/react` (Functional Components), `/vue` (Composition API) e `/astro`. | CP-001 | Alta | 🔄 Em andamento (react/vue via geradores nativos, incluindo `useState`/`v-if`/`v-for` corretos pro AST v1; astro sem gerador nativo na versão pinada 0.14.0 — render estático via `componentToTemplate` + `render-static.ts`, documentado em `.specs/mitosis-compiler-spec.md`, confirmado funcionando com `Show`/`For`/`state`) |
| **BK-001** | Backend | Modelar banco de dados Postgres: `Entity/{Library,Version,User,AuditLog}` (`Component` renomeado pra `Library`, ver ADR-017), migrations com `jsonb` e índices GIN (incluindo GIN+trigram via `pg_trgm` em `Library.name`/`description` pra busca fuzzy). Modelo de versionamento em `.specs/versioning-spec.md`. | Nenhuma | Média | ✅ Concluído (`.specs/backend-architecture-spec.md`, ADR-015/016/017; migration rodada contra Postgres real, índices GIN/trigram confirmados via `\d`; regras de versionamento testadas isoladas — imutabilidade, `currentVersion` só aceita `compiled`, rejeita versão de outra `Library`) |
| **BK-002** | Backend | `Validator/AstSchemaValidator` (não middleware — constraint do Symfony Validator no pipeline padrão de escrita do API Platform) validando `Version.ast` contra `.specs/schemas/component-ast.schema.json` (`opis/json-schema`), garantindo que nenhuma AST inválida ou maliciosa corrompa a base de dados. | BK-001 | Alta | ✅ Concluído (testado com AST real do fixture `Button` — 0 violações — e AST inventada sem `root` — 1 violação com mensagem clara) |
| **BK-003** | Backend | `Storage/MinioArtifactStorage` via `aws/aws-sdk-php` `S3Client` (`use_path_style_endpoint`), gerando URLs pré-assinadas (SigV4/HMAC-SHA256, TTL 180s default) pros artefatos compilados (`.lite.tsx`/react/vue/astro/qa-html) de cada `Version`. | Nenhuma | Média | ✅ Concluído (testado ponta a ponta contra MinIO real: upload, URL pré-assinada com TTL correto, download via HTTP, delete) |
| **BK-004** | Backend | `MessageHandler/CompileVersionHandler` (Symfony Messenger + transporte Redis, `symfony/redis-messenger`) chamando `node compiler/dist/compile.js` via `Process`, reaproveitando o contrato `[ok]`/`[fail]`/`[abort]` já existente. Retry via Messenger (3 tentativas), `UnrecoverableMessageHandlingException` pro `[abort]`. Webhook de status pro Studio é best-effort (Studio não existe ainda). | BK-003 | Alta | ✅ Concluído (testado ponta a ponta: `Version` real compilada de verdade pelo compiler Node, 5 artefatos subidos no MinIO, status `pending`→`compiled`, um artefato baixado de volta e conferido) |
| **BK-005** | Backend | Endpoints REST `/v1/cli/handshake` e `/v1/cli/components/*` como operações customizadas do API Platform (`ApiResource`/`State/Provider`), protegidos por `league/oauth2-server-bundle` por escopo (`ROLE_OAUTH2_*` nativo do bundle — sem Voter customizado, ver ADR-017/guia). | BK-001 | Média | ✅ Concluído (testado ponta a ponta via `$kernel->handle()`: token OAuth2 real emitido, 401 sem token, handshake/list/get/download todos reais, bug real corrigido — `ClientCredentialsUser` não tem `getEmail()`) |
| **INFRA-001** | Infra | Stack de dev local pessoal (Postgres `pgvector/pgvector:pg16` com `pgvector`+`pg_trgm`, Redis, MinIO, proxy reverso por host) em repositório próprio, fora do beaconray, portas isoladas de qualquer outra stack. Produção via Kubernetes, sem docker-compose de prod (ADR-018). | Nenhuma | Média | ✅ Concluído (`docker compose up` real, `pg_trgm`/`vector` confirmados via `\dx`, Redis/MinIO/Traefik respondendo) |
| **WEB-001** | Frontend | `app/frontend`: Astro com `@astrojs/react`+`@astrojs/vue`, componentes estáticos direto via `.astro`, interativos com island React e Vue lado a lado (verificação empírica de paridade visual entre frameworks). | CP-002 | Média | ✅ Concluído (`.specs/component-showcase-spec.md`, ADR-019; testado de verdade via `astro build`+`preview`+Playwright headless — Counter/SaveStatus clicados nas 2 versões, 0 erro/warning no console. 2 bugs reais de SSR achados e corrigidos: `style` string no React, `focusOnShow` sem guard de ref quebrando SSR do Vue) |
| **CP-004** | Compiler | `style` **dinâmico** (`{bind: "<ternário>"}`, usado por `Container`/`Separator`/`Drawer` do `CMP-L1`) tinha o mesmo bug de fundo no React que o `style` literal (string em vez de objeto). | WEB-001 | Média | ✅ Concluído (ADR-019; `compiler/src/fix-react-dynamic-style.ts` — pós-processa o output React, não a camada AST-to-Mitosis, pra não corromper comparação de string dentro da mesma expressão, ex. `props.position === "top"`. Testado com AST sintética reproduzindo esse caso exato + montado como island React real) |
| **CLI-001**| CLI | Criar arquitetura do binário Node.js com TypeScript usando `commander` e `inquirer`. Adicionar lógica de verificação de permissão do sistema operacional e escrita de sessão segura usando armazenamento nativo protegido. | Nenhuma | Média | ⏳ Pendente |
| **CLI-002**| CLI | Desenvolver o comando `npx beaconray add <slug>`. A tarefa exige fazer o download do stream de bytes via Axios da URL assinada do MinIO, verificar a integridade do arquivo via hash SHA256 e descompactar o tarball gravando os arquivos em disco de acordo com o arquivo de configuração do projeto. | BK-005, CLI-001| Alta | ⏳ Pendente |
| **ST-001** | Studio | Desenvolver o wrapper do Canvas interativo utilizando React Context API e `@dnd-kit/core`. Implementar interceptadores de evento de teclado para garantir movimentação de nós por meio das setas direcionais e tecla Enter (Foco de Acessibilidade). | Nenhuma | Alta | ⏳ Pendente |
| **ST-002** | Studio | Criar a interface de parametrização atômica baseada no Figma. Mapear inputs visuais para propriedades HTML, forçando validações onde tipos como `button` exijam obrigatoriamente a propriedade `aria-label` ou `aria-labelledby`. Precisa produzir o mesmo formato de `.specs/ast-component-spec.md` que o compiler (CP-001) já consome. | ST-001 | Média | ⏳ Pendente |
| **QA-001** | Testes | Playwright + `@axe-core/playwright` (camada 1 da estratégia de QA, `.specs/component-qa-strategy-spec.md` + `.specs/qa-automation-spec.md`): renderiza o output `qa-html` do compiler (agora estático de verdade, ver SEO-001) headless, executa `@axe-core` (tags WCAG 2.1/2.2 A/AA, incluindo `target-size`) + checagem de reflow a 320px (SC 1.4.10), gera relatório JSON. | CP-002 | Alta | ✅ Concluído (rodando local via `npm run qa` em `/qa`; 0 violações contra `Button`/`Counter`; sem CI ainda, sem persistência em banco — backend não existe) |
| **QA-002** | Testes | Storybook (camada 2 da estratégia de QA): playground por componente, uma story por variante/estado de prop, addon de a11y (axe-core) e addon de interação (`@storybook/test`, Play function com teclado/clique real). | QA-001 | Média | ⏳ Pendente |
| **QA-003** | Testes | Cypress (camada 3 da estratégia de QA): fluxo real de UI/UX em navegador de verdade, fecha a lacuna de interação que Playwright puro não cobre. | QA-002 | Alta | ⏳ Pendente |
| **QA-004** | Testes | Auditoria via URL: generalizar `qa/src/run.ts` pra aceitar URL arbitrária (não só output local do compiler) — feature de plataforma, usuário aponta qualquer site/componente/app e recebe feedback de SEO/GEO/WCAG. | QA-001 | Alta | ⏳ Pendente (futuro, sem prazo — rastreado em `.specs/qa-automation-spec.md`) |
| **THEME-001** | Qualidade | Beaconray Theme (`.specs/theme-spec.md`): paleta Royal Cyan + fonte Atkinson Hyperlegible, tokens genéricos de CSS (cor/tipografia/espaço/animação) via custom properties puras, foco visível (fecha WCAG 2.4.7), responsividade obrigatória (sem largura fixa em `px`). | QG-001 | Alta | ✅ Concluído (`compiler/theme/tokens.css` + enforcement em `validate.ts`; contraste real confirmado via axe-core — 6.57:1 medido, bate com o cálculo) |
| **PLAT-001** | Plataforma | Marketplace público: publicar tema/componente (do repo próprio via CLI ou direto do Studio), tornar público, ranking social (estrelas, comentários, downloads) — estilo GitHub. | BK-001, BK-005, CLI-002, ST-002 | Alta | ⏳ Pendente (futuro, sem prazo) |
| **CAT-001** | Catálogo | Escrever `.specs/component-catalog-spec.md`: lista v1 fechada (34 componentes, 3 camadas) + convenção de padrão por categoria (form control, overlay, estrutural, navegação). | HN-001 | Média | ✅ Concluído (ADR-012) |
| **CMP-L1** | Componentes | Construir a Camada 1 (base estrutural): `Card`, `Container` (grid embutido), `Separator`, `Drawer` (posição configurável), `Modal` (header/footer internos, backdrop, empilhamento). | CAT-001, CP-002 | Alta | ⏳ Pendente |
| **CMP-L2** | Componentes | Construir a Camada 2 (interativos): `Link`, `Button` (✅ já existe), `Input` (label flutuante), `Switcher`, `Dropdown` (busca + infinite scroll assíncrono), `Date Picker`, `Accordion`, `Popover`, `Toast`, `Tooltip` (troca de foco sem flicker), `Breadcrumb`, `Paginator`, `Avatar`, `Badge`, `Progress bar`, `Spinner`, `File Upload/Dropzone`, `Color Picker`. | CAT-001, CP-002 | Alta | ⏳ Pendente |
| **CMP-L3** | Componentes | Construir a Camada 3 (complexos, em cima da base): `Text/Content Editor`, `Carousel`, `DataTable`, `Stepper/Wizard`, `Chart`, `Navbar`, `Resizable`, `Tabs`, `Tree View`, `Command Palette`, `Calendar`. | CMP-L1, CMP-L2 | Crítica | ⏳ Pendente |

---

## 📈 Métricas de Progresso
*   **Total de Tarefas:** 29
*   **Concluídas:** 17 (HN-001, QG-001, QG-002, CP-001, QA-001, SEO-001, SEO-002, THEME-001, CAT-001, BK-001, BK-002, BK-003, BK-004, BK-005, INFRA-001, WEB-001, CP-004)
*   **Em andamento:** 1 (CP-002)
*   **Complexidade Crítica / Alta:** 15
*   **Estimativa de Esforço Base:** 160h-220h de engenharia focada (não recalculado desde a adição de HN-001/QG-001/QG-002/QA-002/QA-003/SEO-001/SEO-002/QA-004/THEME-001/PLAT-001/CAT-001/CMP-L1/CMP-L2/CMP-L3/INFRA-001/WEB-001/CP-004 — revisar na próxima rodada de planejamento; BK-001..005 adiantado da Fase 3 concluído nesta rodada, ver ADR-015).
